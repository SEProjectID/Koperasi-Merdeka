name: Build APK Koperasi Merdeka

on:
  workflow_dispatch:
    inputs:
      app_url:
        description: "Alamat web koperasi (kosongkan untuk pakai default di app.config.json)"
        required: false
        default: ""
      app_name:
        description: "Nama aplikasi"
        required: false
        default: "Koperasi Merdeka"
      version_code:
        description: "Nomor versi (angka, naikkan tiap rilis baru)"
        required: false
        default: "1"
      version_name:
        description: "Nama versi (contoh 1.0.0)"
        required: false
        default: "1.0.0"
      release:
        description: "Build APK rilis bertanda tangan (butuh Secrets keystore)"
        type: boolean
        required: false
        default: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
    steps:
      - name: Ambil kode
        uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Siapkan Android SDK (pakai yang sudah ada di runner, tanpa unduh ulang)
        run: |
          SDKMGR="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"
          if [ ! -f "$SDKMGR" ]; then
            SDKMGR=$(find "$ANDROID_SDK_ROOT/cmdline-tools" -maxdepth 2 -name sdkmanager | head -n1)
          fi
          echo "Pakai sdkmanager: $SDKMGR"
          yes | "$SDKMGR" --licenses > /dev/null || true
          "$SDKMGR" "platform-tools" "platforms;android-34" "build-tools;34.0.0"

      - uses: gradle/actions/setup-gradle@v4

      - name: Pasang dependensi
        run: npm ci

      - name: Siapkan proyek Android (alamat, ikon, splash)
        env:
          APP_URL: ${{ inputs.app_url }}
          APP_NAME: ${{ inputs.app_name }}
        run: npm run setup

      - name: Keystore (hanya untuk rilis)
        if: ${{ inputs.release }}
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          if [ -z "$KEYSTORE_BASE64" ]; then echo "Secret KEYSTORE_BASE64 belum diisi"; exit 1; fi
          echo "$KEYSTORE_BASE64" | base64 -d > "$RUNNER_TEMP/release.jks"
          echo "KEYSTORE_FILE=$RUNNER_TEMP/release.jks" >> "$GITHUB_ENV"

      - name: Build APK
        working-directory: android
        env:
          VERSION_CODE: ${{ inputs.version_code }}
          VERSION_NAME: ${{ inputs.version_name }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          chmod +x gradlew
          if [ "${{ inputs.release }}" = "true" ]; then
            ./gradlew assembleRelease --no-daemon
          else
            ./gradlew assembleDebug --no-daemon
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: KoperasiMerdeka-APK
          path: android/app/build/outputs/apk/**/*.apk
          if-no-files-found: error
